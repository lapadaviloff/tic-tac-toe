#ifndef CLASSBOARD_H_
#define CLASSBOARD_H_
class classboard{
 private:
  int board[5][5];
  static const int FILED_SIZE;
  int isWin;
  int noWin;
  int maxcol;// количество ячеекy
 public:
  classboard();
  bool makemove(int x,int y, int sign);
  int getSize();
  int getisWin();
  int getcell(int x, int y);
  int getnoWin();
  
};


#endif
